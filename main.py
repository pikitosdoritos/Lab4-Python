# 1
# Сформувати список list_1 із цілих чисел із діапазону [0 ,20] із кроком 2
# із використанням функції range() та конструктора list(). Виконати такі дії
# вивести елементи з непарними індексами;
# вивести елементи з початку і до 5-го включно, з 3-го і до
# передостаннього включно з кроком 2;
# вивести елементи списку у зворотному порядку із використанням
# слайдів та із використанням методів списків;
# поміняти місцями перший та останній елементи списку із
# використанням індексів та із використанням методів списків;
# перевірити, чи є число 0 у списку

print("==" * 80 + "\n")
print("Завдання 1\n")
print("==" * 80 + "\n")

list_1 = list(range(0, 21, 2))

print(f"Список з цілих чисел із діапазону [0 ,20] із кроком 2: {list_1}\n")
print(f"Елементи з непарними індексами: {list_1[1::2]}\n")
print(f"Елементи з початку і до 5-го включно: {list_1[:6]}\n")
print(f"Елементи списку з 3-го і до передостаннього включно з кроком 2: {list_1[3:-1:2]}\n")
print(f"Елементи списку у зворотному порядку із використанням слайсів: {list_1[::-1]}\n")
print(f"Елементи списку у зворотному порядку із використанням методів списків: {list(reversed(list_1))}\n")

list_copy = list_1.copy()

list_copy[0], list_copy[-1] = list_copy[-1], list_copy[0]

print(f"Поміняти місцями перший та останній елементи списку із використанням індексів: {list_copy}\n")

first = list_1.pop(0)
last = list_1.pop(-1)

list_1.insert(0, last)
list_1.append(first)

print(f"Поміняти місцями перший та останній елементи списку із використанням методів списків: {list_1}\n")
print(f"Перевірити, чи є число 0 у списку: {0 in list_1}\n")

# 2
# Сформувати список list_2 із 10 випадкових цілих чисел із діапазону [-
# 7,7] із використанням бібліотеки random. Виконати такі дії
# вивести мінімальний та максимальний елементи списку;
# знайти довжину списку та суму його елементів;
# знайти індекси мінімального та максимального елементів;
# поміняти місцями максимальний та мінімальний елементи;
# вивести відсортовану копію списку

print("==" * 80 + "\n")
print("Завдання 2\n")
print("==" * 80 + "\n")

import random

list_2 = [random.randint(-7, 7) for _ in range(10)]

print(f"Список з 10 випадкових цілих чисел із діапазону [-7,7]: {list_2}\n")

min_item = min(list_2)
max_item = max(list_2)

print(f"Мінімальний елемент списку: {min_item}\n")
print(f"Максимальний елемент списку: {max_item}\n")
print(f"Довжина списку: {len(list_2)}\n")
print(f"Сума елементів списку: {sum(list_2)}\n")
print(f"Індекс мінімального елемента: {list_2.index(min_item)}\n")
print(f"Індекс максимального елемента: {list_2.index(max_item)}\n")

list_2[list_2.index(min_item)] = max_item
list_2[list_2.index(max_item)] = min_item

print(f"Поміняти місцями максимальний та мінімальний елементи: {list_2}\n")

sorted_list = sorted(list_2)

print(f"Відсортована копія списку: {sorted_list}\n")

# 3 
# Сформувати список з list_3, що складається з елементів списку list_2 та
# елементів рядку “A b Bd 12a057cdF#”.
# додати в list_3 елемент „@‟ після елементу “B” в кінець списку;
# видалити 3-й та останній елементи списку;
# знайти кількість входжень елементу 0;
# знайти індекси першого входження елементу 0;
# перетворити список list_3 на рядок із розподільником “, ”;

print("==" * 80 + "\n")
print("Завдання 3\n")
print("==" * 80 + "\n")

string = "A b Bd 12a057cdF#"

list_3 = list_2 + list(string)

print(f"Список з елементами списку list_2 та рядку \"A b Bd 12a057cdF#\": {list_3}\n")

index_B = list_3.index("B")
list_3.insert(index_B + 1, "@")

print(f"Додати в list_3 елемент \"@\" після елементу \"B\" в кінець списку: {list_3}\n")
print(f"Видалити 3-й та останній елементи списку: {list_3.pop(3)} та {list_3.pop(-1)}\n")
print(f"Кількість входжень елементу 0: {list_3.count(0)} раз\n")
print(f"Індекси першого входження елементу 0: {list_3.index(0)}\n")
print(f"Перетворити список list_3 на рядок із розподільником \", \": {', '.join(map(str, list_3))}\n")

# 4
# Для списку list_3 виконати такі дії із використанням спискових
# включень та/або функціq map():
# сформувати з нього список list_4, що складається тільки із чисел;
# вивести всі додатні елементи списку list_4 із використанням спискових
# включень,
# виконати попередній пункт за допомогою функцій map(), filter();
# створити список квадратів елементів списку list_4
# вивести елементи списку list_3, які є літерами, у верхньому регістрі із
# використанням спискових включень;
# виконати попередній пункт за допомогою функцій map(), filter().

print("==" * 80 + "\n")
print("Завдання 4\n")
print("==" * 80 + "\n")

list_4 = [x for x in list_3 if isinstance(x, int)]
print(f"Список тільки з чисел: {list_4}\n")

positive = [x for x in list_4 if x > 0]
print(f"Додатні елементи (list comprehension): {positive}\n")

positive_map = list(filter(lambda x: x > 0, list_4))
print(f"Додатні елементи (map/filter): {positive_map}\n")

squares = [x**2 for x in list_4]
print(f"Квадрати елементів: {squares}\n")

letters_upper = [x.upper() for x in list_3 if isinstance(x, str) and x.isalpha()]
print(f"Літери у верхньому регістрі: {letters_upper}\n")

letters_upper_map = list(map(str.upper, filter(lambda x: isinstance(x, str) and x.isalpha(), list_3)))
print(f"Літери у верхньому регістрі (map/filter): {letters_upper_map}\n")

# 5
# Сформувати список з list_5 із 12 елементів: 8 цілих чисел та 4-х рядків.
# На основі цього списку створити інший список list_6 з 8 числових
# елементів, в якому кожен елемент дорівнює середньоарифметичному усіх
# наступних числових елементів списку list_5 (з точністю до двох знаків).
# Наприклад list_1 = [1, 1, “ASbvsxT”, 2, 4, “A”, -1, „qq‟, 7, 2, 0, “qweT”].
# На виході маємо
# list_2 =[2.00, 2.14, 2.33, 2.40, 2.00, 3.00, 1.00, 0.00].
# Другий елемент (2.14) - це середньоарифметичне чисел 1, 2, 4, -1, 7, 2, 0.
# Шостий елемент 3.00 - середньоарифметичне чисел (7, 2, 0)
# На основі list_5 створити список рядків, та вивести рядки з довжиною
# більше 2.

print("==" * 80 + "\n")
print("Завдання 5\n")
print("==" * 80 + "\n")

list_5 = [1, 1, "ASbvsxT", 2, 4, "A", -1, "qq", 7, 2, 0, "qweT"]

print(f"Початковий список list_5: {list_5}\n")

numbers = [x for x in list_5 if isinstance(x, int)]

list_6 = []

for i in range(len(numbers)):
    tail = numbers[i+1:]
    if tail:
        avg = round(sum(tail) / len(tail), 2)
    else:
        avg = 0.00
    list_6.append(avg)

print(f"Список list_6 (середні значення): {list_6}\n")

strings = [x for x in list_5 if isinstance(x, str)]
print(f"Список рядків: {strings}\n")

long_strings = [x for x in strings if len(x) > 2]
print(f"Рядки з довжиною більше 2: {long_strings}\n")